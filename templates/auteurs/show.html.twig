{% extends 'base.html.twig' %}

{% block title %}{{ auteur.prenom }} {{ auteur.nom }} - Bibliothèque des Enfants{% endblock %}

{% block body %}
    <div class="author-header">
        <div class="author-actions">
            <a href="{{ path('app_auteurs') }}" class="btn-back">
                <span class="arrow-left">←</span> Retour aux conteurs
            </a>
        </div>
        
        <div class="author-profile">
            <div class="author-avatar">
                <span class="avatar-text">{{ auteur.prenom|first }}{{ auteur.nom|first }}</span>
            </div>
            <div class="author-details">
                <h1 class="author-name">{{ auteur.prenom }} {{ auteur.nom }}</h1>
                <div class="book-count">
                    <span class="book-icon">📚</span>
                    <span>{{ auteur.livre|length }} {% if auteur.livre|length > 1 %}livres{% else %}livre{% endif %}</span>
                </div>
            </div>
        </div>
        
        <div class="author-description">
            <div class="magic-wand">✨</div>
            <p>Chaque histoire racontée par {{ auteur.prenom }} est une porte vers un monde imaginaire extraordinaire !</p>
            <div class="magic-wand">✨</div>
        </div>
    </div>
    
    <h2 class="section-title">Aventures créées par ce magicien</h2>
    
    {% if auteur.livre|length > 0 %}
        <div class="card-grid">
            {% for livre in auteur.livre %}
                <div class="card">
                    <div class="card-image">
                        {% if livre.couverture %}
                            <a href="{{ path('app_livre_show', {'id': livre.id}) }}">
                                <img src="{{ asset('uploads/couvertures/' ~ livre.couverture) }}" alt="Couverture {{ livre.titre }}">
                            </a>
                        {% else %}
                            <div class="no-cover">
                                <span>📕</span>
                            </div>
                        {% endif %}
                    </div>
                    <div class="card-content">
                        <h3 class="card-title">{{ livre.titre }}</h3>
                        <p class="card-excerpt">
                            {% if livre.resume %}
                                {{ livre.resume|striptags|length > 80 ? livre.resume|striptags|slice(0, 80) ~ '...' : livre.resume|striptags }}
                            {% else %}
                                Une histoire magique t'attend...
                            {% endif %}
                        </p>
                        <a href="{{ path('app_livre_show', {'id': livre.id}) }}" class="btn-discover">Lire l'aventure</a>
                    </div>
                </div>
            {% endfor %}
        </div>
    {% else %}
        <div class="empty-books">
            <img src="data:image/svg+xml,%3Csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 512 512'%3E%3Cpath fill='%23ff6b6b' d='M325.13 34.89c-27.9-3.4-56.43-5.56-85.13-5.56-28.7 0-57.23 2.16-85.13 5.56C130.35 37.47 110 40.63 90 44.47v16.86c20-4.09 40.35-7.38 64.87-10.06 27.32-3.32 55.22-5.43 82.24-5.43 0 28.96 0 339.21 0 367.51-27.02 0-54.92-2.11-82.24-5.43-24.52-2.68-44.88-5.97-64.87-10.06v16.86c19.99 3.84 40.35 7 54.87 9.58 27.9 3.4 56.43 5.56 85.13 5.56 28.7 0 57.23-2.16 85.13-5.56 14.53-2.58 34.89-5.74 54.87-9.58v-16.86c-20 4.09-40.35 7.38-64.87 10.06-27.32 3.32-55.22 5.43-82.24 5.43 0-28.96 0-339.21 0-367.51 27.02 0 54.92 2.11 82.24 5.43 24.52 2.68 44.88 5.97 64.87 10.06V44.47c-19.99-3.84-40.35-7-54.87-9.58z'/%3E%3C/svg%3E" class="empty-icon">
            <p>Oh ! {{ auteur.prenom }} est en train d'écrire de nouvelles aventures...</p>
            <p>Reviens bientôt pour découvrir les histoires de ce conteur magique !</p>
        </div>
    {% endif %}

    <style>
        .author-header {
            margin-bottom: 40px;
        }
        
        .author-actions {
            margin-bottom: 30px;
        }
        
        .btn-back {
            display: inline-flex;
            align-items: center;
            color: white;
            background-color: var(--primary-color);
            text-decoration: none;
            font-size: 16px;
            font-weight: bold;
            transition: var(--transition);
            padding: 10px 20px;
            border-radius: 30px;
            box-shadow: 0 4px 0 #e55757;
            position: relative;
            top: 0;
        }
        
        .btn-back:hover {
            background-color: var(--secondary-color);
            transform: translateY(-3px);
            box-shadow: 0 7px 0 #3cc4e6;
        }
        
        .btn-back:active {
            top: 3px;
            box-shadow: 0 1px 0 #3cc4e6;
        }
        
        .arrow-left {
            margin-right: 5px;
        }
        
        .author-profile {
            display: flex;
            align-items: center;
            margin-bottom: 30px;
        }
        
        .author-avatar {
            width: 100px;
            height: 100px;
            background-color: var(--accent-color);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin-right: 25px;
            flex-shrink: 0;
            color: white;
            font-size: 42px;
            font-weight: bold;
            text-shadow: 1px 1px 0 rgba(0,0,0,0.1);
            box-shadow: 0 6px 15px rgba(0,0,0,0.1);
        }
        
        .author-details {
            flex: 1;
        }
        
        .author-name {
            color: var(--primary-color);
            font-size: 32px;
            margin: 0 0 10px 0;
            font-weight: bold;
            text-shadow: 2px 2px 0 rgba(0,0,0,0.05);
        }
        
        .book-count {
            display: flex;
            align-items: center;
            gap: 8px;
            color: var(--secondary-color);
            font-weight: bold;
            font-size: 18px;
        }
        
        .book-icon {
            font-size: 24px;
        }
        
        .author-description {
            background-color: white;
            border-radius: 20px;
            padding: 20px;
            margin-bottom: 40px;
            text-align: center;
            display: flex;
            align-items: center;
            justify-content: center;
            border: 3px solid var(--accent-color);
            box-shadow: var(--box-shadow);
        }
        
        .author-description p {
            font-size: 18px;
            color: var(--dark-color);
            margin: 0 15px;
            font-weight: bold;
        }
        
        .magic-wand {
            font-size: 24px;
            animation: sparkle 2s infinite alternate;
        }
        
        @keyframes sparkle {
            0% { transform: rotate(-10deg) scale(0.9); opacity: 0.7; }
            100% { transform: rotate(10deg) scale(1.1); opacity: 1; }
        }
        
        .section-title {
            text-align: center;
            margin-bottom: 30px;
        }
        
        .card-excerpt {
            color: var(--dark-color);
            font-size: 14px;
            margin: 10px 0 15px;
            line-height: 1.4;
        }
        
        .btn-discover {
            display: block;
            background-color: var(--accent-color);
            color: white;
            padding: 8px;
            border-radius: 20px;
            text-decoration: none;
            text-align: center;
            font-weight: bold;
            font-size: 14px;
            transition: all 0.3s;
            box-shadow: 0 3px 0 #e6ac2e;
        }
        
        .btn-discover:hover {
            transform: translateY(-3px);
            box-shadow: 0 6px 0 #e6ac2e;
        }
        
        .empty-books {
            text-align: center;
            padding: 40px 20px;
            background-color: white;
            border-radius: 20px;
            margin: 30px 0;
            border: 3px dashed var(--accent-color);
        }
        
        .empty-icon {
            width: 80px;
            height: 80px;
            margin-bottom: 20px;
            opacity: 0.7;
        }
        
        .empty-books p {
            font-size: 18px;
            color: var(--dark-color);
            margin: 10px 0;
            font-weight: bold;
        }
        
        @media (max-width: 768px) {
            .author-profile {
                flex-direction: column;
                text-align: center;
            }
            
            .author-avatar {
                margin-right: 0;
                margin-bottom: 20px;
            }
            
            .author-description {
                flex-direction: column;
                gap: 10px;
                padding: 15px;
            }
            
            .author-description p {
                font-size: 16px;
            }
            
            .magic-wand {
                font-size: 20px;
            }
            
            .author-name {
                font-size: 26px;
            }
            
            .book-count {
                justify-content: center;
            }
        }
    </style>
{% endblock %} 